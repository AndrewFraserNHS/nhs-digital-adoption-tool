// PLACEHOLDER - Will restore full file
// For now, creating minimal placeholder to get build working
export function mountAdoptionTool(container: HTMLElement) {
  container.innerHTML = '<p>AdoptionToolPage is being restored</p>';
}

export default { mountAdoptionTool };
